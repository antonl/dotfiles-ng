# this is an upstream repo
[$HOME/.config/vcsh/repo.d/oh-my-zsh.git]
order = 10
checkout = 
     mkdir -p ~/.oh-my-zsh;
     export VCSH_BASE=$HOME/.oh-my-zsh;
     vcsh clone "git@github.com:ohmyzsh/ohmyzsh.git" oh-my-zsh
